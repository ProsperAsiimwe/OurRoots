@model OurRoots.Domain.Entities.GalleryUpload

<!-- Section:  -->
<section>
    <div class="container pb-30">
        <div class="section-content">
            <div class="row">
                <div class="col-md-12">
                    <h3 class="line-bottom">Are you sure you want to delete this?</h3>

                    @Html.Bootstrap().ActionLinkButton("Back", "Index", "GalleryUpload").Style(ButtonStyle.Success)

                    <hr />

                    <dl class="dl-horizontal">
                        <dt>
                            @Html.DisplayNameFor(model => model.GalleryUploadId)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.GalleryUploadId)
                        </dd>
                                              
                      
                        <dt>
                            @Html.DisplayNameFor(model => model.FolderPath)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.FolderPath)
                        </dd>


                    </dl>

                    @using (Html.BeginForm("DeleteConfirmed", "GalleryUpload", new { id = Model.GalleryUploadId }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.Bootstrap().SubmitButton().Text("Delete Photo").Style(ButtonStyle.Danger)

                    }


                </div>

            </div>
        </div>
    </div>
</section>

